import { IntroSlider, Prices, SupportDevice } from "../../components/Infos";

const HomePage = () => {
	return (
		<section>
			<IntroSlider />
			<SupportDevice />
			<Prices />
		</section>
	);
};

export default HomePage;
