import { PageHeader } from "../../components/Common";

const TVProgramPage = () => {
	return (
		<section className="pageSection">
			<PageHeader />
		</section>
	);
};

export default TVProgramPage;
